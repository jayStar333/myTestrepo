import pandas as pd

# Read the Excel file
file_path = 'your_excel_file.xlsx'
df = pd.read_excel(file_path, header=1)  # Headers are in the second row (index 1)

# Convert all column headers to string
df.columns = df.columns.astype(str)

# Select the columns "VDS" and "SQLTable" and those ending with "Dremio difference"
selected_columns = ['VDS', 'SQLTable'] + [col for col in df.columns if col.endswith('Dremio difference')]

# Filter rows that have at least one entry in any of the "Dremio difference" columns
filtered_df = df[df[[col for col in df.columns if col.endswith('Dremio difference')]].notna().any(axis=1)]

# Select only the required columns
result_df = filtered_df[selected_columns]

# Save the result to a new Excel file
result_df.to_excel('filtered_data.xlsx', index=False)
